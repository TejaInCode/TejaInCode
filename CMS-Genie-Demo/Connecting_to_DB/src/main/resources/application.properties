spring.datasource.url=jdbc:mysql://localhost:3306/cmsdemo1
spring.datasource.username=root
spring.datasource.password=teja
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true




#   spring.jpa.hibernate.ddl-auto=update        ---->It updates the database schema to match your entity classes without dropping existing tables or data.
#                                                       New fields or tables are added, but existing data and structure remain safe.

#   spring.jpa.hibernate.ddl-auto=Create        ----->here create do the job like it will drop the already created table and again it will create the table if the userdetails is mentioned.
#                                                       like when the computer is running the data will there when ever we off the data will gone.

#   spring.jpa.show-sql=true                    ---->It enables logging of the SQL queries generated by Hibernate.
#                                                       You can see the queries in the console when the application runs.

